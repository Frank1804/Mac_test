version: 2.0
jobs:
  build:
    docker:
      - image: circleci/ruby:2.4.2-jessie-node
    steps:
      - checkout
      - run: echo "hello wooooooorld -It's not THAT bad. This was the locally pushed version"
